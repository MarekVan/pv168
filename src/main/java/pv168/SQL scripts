CREATE TABLE account (
id BIGINT NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
owner VARCHAR (200),
balance DECIMAL
)

CREATE TABLE payment (
id bigint not null primary key generated always as identity,
amount DECIMAL,
fromAcc BIGINT,
toAcc BIGINT,
dateSent DATE 
)